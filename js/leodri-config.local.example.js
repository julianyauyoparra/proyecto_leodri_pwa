/**
 * Copia como js/leodri-config.local.js (solo tu PC; no subir a cPanel).
 * Se carga después de leodri-config.js y sobrescribe valores para desarrollo local.
 */
window.LEODRI_CONFIG = Object.freeze({
    whatsapp: '51935486809',
    stockApiUrl: 'http://localhost:3000/api/stock'
});
